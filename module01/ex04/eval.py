class Evaluator:

    @staticmethod
    def zip_evaluate(words: 'list[str]', coefs: 'list[int]') -> int:
        if len(words) != len(coefs):
            return -1
        # Use zip here

        pass

    @staticmethod
    def enumerate_evaluate(words: 'list[str]', coefs: 'list[int]'):
        if len(words) != len(coefs):
            return -1
        # Use enumerate here
        pass
